---
name: Check pre commit
on:
  pull_request:
    types: [edited, opened, reopened, synchronize]
jobs:
  pre-commit:
    name: Check pre commit
    permissions:
      contents: read
    uses:
      ministryofjustice/data-engineering-github-actions/.github/workflows/reusable-pre-commit.yml@bc3387b3bdd728d1c7e26e7f576929e4957507be  # v1.1.0
    with:
      include_terraform: true
